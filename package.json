{
  "name": "nexus-chat",
  "version": "1.0.0",
  "description": "Secure self-hosted gamer chat alternative",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client --include=dev",
    "build": "npm install --prefix server && npm install --prefix client --include=dev && npm --prefix client run build",
    "build:client": "npm install --prefix client --include=dev && npm --prefix client run build",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}